export type Post = {
    title: string,
    slug: string,
    date: string,
    author: string,
    authorTwitter: string,
    category?: string,
    tags?: string[],
    description: string,
    body: string
}
